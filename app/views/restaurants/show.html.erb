<div class="container">
  <div class="card text-center">
    <h1 class="text-success">Restaurant <%= @restaurant.name %></h1>
    <h4>Location: <%= @restaurant.address %></h4>
    <h4>Phone Number: <%= @restaurant.phone_number %></h4>
    <h4>Food Category: <%= @restaurant.category %></h4>
    <br>
    <h4>Reviews:</h4>
    <ul class="list">
    <% @restaurant.reviews.each do |review| %>
      <li><%= review.content%> - <%= review.rating %></li>
    <% end %>
    </ul>
  <div class="d-flex justify-content-center">
      <%= link_to "Añade un review", new_restaurant_review_path(@restaurant), class:"btn btn-primary" %>
      <%= link_to "Homepage", restaurants_path, class:"btn btn-success ml-3" %>
  </div>
  </div>
</div>